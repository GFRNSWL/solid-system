# 摸鱼贪吃蛇

这是一个单文件的静态网页小游戏，放在 GitHub Pages 上就能直接访问。

## 运行入口
- `index.html`

## 操作
- 方向键 / `WASD`：移动
- `空格`：暂停 / 继续
- `R`：重新开始

## 发布到 GitHub Pages
1. 打开仓库 `Settings`
2. 找到 `Pages`
3. 选择 `Deploy from a branch`
4. Branch 选 `main`，folder 选 `/ (root)`
5. 保存后等待 GitHub 生成站点

发布完成后，你可以把网址发给朋友直接在浏览器里玩。